v1.1
* Some bugs fixed
- Option names removed
+ Added changelog (you're seeing it right now .-. )
* "algorithm.dat" file changed to a folder called "alg". The main algorithm is now called "start.alg"
+ File imports in an algorithm implemented (with '@' symbol)
+ Commentaries added (with '*' symbol)
+ Now the documentation is in README.md file

v1.0
+ First release! :D